extends ../base/main

block variables
    - var pageTitle = 'Main Page'
    - var youAreUsingPug = true
    - var pclass = 'paragraph-class'

block content
    h1 Pug - node template engine
    #container.col
        if youAreUsingPug
            p(class=pclass) You are amazing
        else
            p Get on it!
        p.
            Pug is a terse and simple templating language with a
            strong focus on performance and powerful features.
