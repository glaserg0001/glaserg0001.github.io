-  var URL_Pug_MainNav = function(file, index = false) {
-    return '/app/' + file + (index ? '/' : '.html')
-  }


- var URL_CSS_file = function(file, lvl = 0) {
-   var cssDir = ''
-   while (lvl > 0) {
-     cssDir = cssDir + '../'
-     lvl--
-   }
-   return cssDir + `css/${file}.css`
- }
